# L04 Pure Top10 List Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `L04A / L04B` 从“Top1 主角卡 + Top2~Top10 列表”改成“纯 Top10 列表”，并同步把 sample contract 补齐到 10 条。

**Architecture:** 保持现有 Python 年报 contract 与 `payload.ranking` 结构不变，只改 `tools/year-report-app` 的页面解释层、前端测试与 sample JSON。先用 Vitest 把“渲染 10 条纯列表”行为钉死，再用最小 Vue 组件与样式改动让测试转绿，最后补 sample 数据并跑构建验证 fallback 一致性。

**Tech Stack:** Vue 3、Vitest、Vite、JSON

---

## 一、文件职责与改动范围

### 预计修改文件

- `tools/year-report-app/src/__tests__/year-report-mobile-app.test.js`
  - 把 `L04A/L04B` 页面断言从 hero + 9 条列表改成纯 10 条列表；
  - 保留 Top10 截断断言，移除对 hero 区的依赖。

- `tools/year-report-app/src/pages/L04LibraryArtistRankingPage.vue`
  - 移除 `champion / runnerUps` 拆分；
  - 改成直接渲染 `payload.ranking.slice(0, 10)` 的连续 10 条榜单。

- `tools/year-report-app/src/pages/L04NewArtistRankingPage.vue`
  - 移除 `champion / runnerUps` 拆分；
  - 改成直接渲染 `payload.ranking.slice(0, 10)` 的连续 10 条榜单。

- `tools/year-report-app/src/styles.css`
  - 为纯列表模式补轻量样式；
  - 给第 1 名保留列表内轻强调，不再使用独立 hero 卡。

- `tools/year-report-app/public/report-contract.sample.json`
  - 把 `L04A / L04B` 的样例榜单都补到 10 条；
  - 保持字段结构与 rank 连续性。

---

## 二、任务分解

### Task 1: 先用前端测试钉死“纯 Top10 列表”行为

**Files:**
- Modify: `tools/year-report-app/src/__tests__/year-report-mobile-app.test.js`
- Test: `tools/year-report-app/src/pages/L04LibraryArtistRankingPage.vue`
- Test: `tools/year-report-app/src/pages/L04NewArtistRankingPage.vue`

- [ ] **Step 1: 把 `L04A` 测试改成断言纯列表 10 条，不再依赖 hero 区**

```javascript
it('L04A 改成纯 Top10 列表，并且只渲染前 10 名', () => {
  const wrapper = mount(L04LibraryArtistRankingPage, {
    props: {
      page: {
        page_id: 'L04A',
        section: '歌手章节',
        title: '歌曲库歌手榜',
        summary_text: '你常驻收藏最多的歌手，在这一页完整展开。',
        payload: {
          ranking: createArtistRanking('馆藏歌手 ', 'track_total', '首收藏'),
        },
      },
    },
  })

  expect(wrapper.find('.artist-ranking-page').exists()).toBe(true)
  expect(wrapper.find('.hero-subtitle').text()).toBe('全曲库收藏最多的 10 位歌手')
  expect(wrapper.find('.artist-ranking-list-card').exists()).toBe(true)
  expect(wrapper.findAll('.artist-ranking-list-item')).toHaveLength(10)
  expect(wrapper.text()).toContain('馆藏歌手 1')
  expect(wrapper.text()).toContain('馆藏歌手 10')
  expect(wrapper.text()).not.toContain('馆藏歌手 11')
  expect(wrapper.find('.page-summary-card').exists()).toBe(true)
})
```

- [ ] **Step 2: 把 `L04B` 测试改成断言纯列表 10 条，不再依赖 hero 区**

```javascript
it('L04B 改成纯 Top10 列表，并且只渲染前 10 名', () => {
  const wrapper = mount(L04NewArtistRankingPage, {
    props: {
      page: {
        page_id: 'L04B',
        section: '歌手章节',
        title: '年度新增歌手榜',
        summary_text: '这一年你不断扩充歌手版图，新面孔都在这里。',
        payload: {
          ranking: createArtistRanking('新增歌手 ', 'new_track_total', '首新增'),
        },
      },
    },
  })

  expect(wrapper.find('.artist-ranking-page').exists()).toBe(true)
  expect(wrapper.find('.hero-subtitle').text()).toBe('今年扩坑最多的 10 位歌手')
  expect(wrapper.find('.artist-ranking-list-card').exists()).toBe(true)
  expect(wrapper.findAll('.artist-ranking-list-item')).toHaveLength(10)
  expect(wrapper.text()).toContain('新增歌手 1')
  expect(wrapper.text()).toContain('新增歌手 10')
  expect(wrapper.text()).not.toContain('新增歌手 11')
  expect(wrapper.find('.page-summary-card').exists()).toBe(true)
})
```

- [ ] **Step 3: 跑前端单测，确认先红**

Run:
```bash
npm run test -- src/__tests__/year-report-mobile-app.test.js
```

Workdir: `tools/year-report-app`

Expected:
- FAIL
- 失败点集中在 `L04A / L04B` 列表数量从 9 变 10 的新断言上。

---

### Task 2: 用最小组件改动把 L04A/L04B 调整为纯列表

**Files:**
- Modify: `tools/year-report-app/src/pages/L04LibraryArtistRankingPage.vue`
- Modify: `tools/year-report-app/src/pages/L04NewArtistRankingPage.vue`
- Test: `tools/year-report-app/src/__tests__/year-report-mobile-app.test.js`

- [ ] **Step 1: 在 `L04LibraryArtistRankingPage.vue` 直接渲染 `topTenRanking`，去掉 `champion / runnerUps`**

```vue
<template>
  <ReportPageShell :page="page">
    <div class="artist-ranking-page hero-layout">
      <div class="hero-copy hero-copy--editorial">
        <span class="hero-tag hero-tag-pill">Library artist ranking</span>
        <h2 class="hero-title">{{ page.title }}</h2>
        <p class="hero-subtitle">全曲库收藏最多的 10 位歌手</p>
      </div>

      <section v-if="topTenRanking.length" class="artist-ranking-list-card ranking-panel ranking-panel--accent">
        <header class="panel-header">
          <h3>Top 1 - Top 10</h3>
          <span>{{ topTenRanking.length }} 位歌手</span>
        </header>
        <ol class="ranking-list artist-ranking-list">
          <li
            v-for="(item, index) in topTenRanking"
            :key="`library-${item.artist_display || index}`"
            :class="['artist-ranking-list-item', 'ranking-item', { 'artist-ranking-list-item--top': index === 0 }]"
          >
            <div class="artist-ranking-item-main">
              <span class="artist-ranking-list-rank">#{{ item.rank || index + 1 }}</span>
              <div class="artist-ranking-item-copy">
                <strong>{{ item.artist_display || '未知歌手' }}</strong>
                <small>专辑 {{ item.album_total ?? 0 }} 张 · 代表作 {{ item.top_track_title || '未知歌曲' }}</small>
              </div>
            </div>
            <span>{{ formatMetric(item) }}</span>
          </li>
        </ol>
      </section>
      <section v-else class="artist-ranking-list-card story-card story-card--focus">
        <strong>榜单暂未生成</strong>
        <p>等歌手统计补齐后，这里会出现你的曲库 Top10 歌手榜。</p>
      </section>
    </div>
  </ReportPageShell>
</template>
```

- [ ] **Step 2: 在 `L04NewArtistRankingPage.vue` 直接渲染 `topTenRanking`，去掉 `champion / runnerUps`**

```vue
<template>
  <ReportPageShell :page="page">
    <div class="artist-ranking-page hero-layout">
      <div class="hero-copy hero-copy--editorial">
        <span class="hero-tag hero-tag-pill">New artist ranking</span>
        <h2 class="hero-title">{{ page.title }}</h2>
        <p class="hero-subtitle">今年扩坑最多的 10 位歌手</p>
      </div>

      <section v-if="topTenRanking.length" class="artist-ranking-list-card ranking-panel">
        <header class="panel-header">
          <h3>Top 1 - Top 10</h3>
          <span>{{ topTenRanking.length }} 位歌手</span>
        </header>
        <ol class="ranking-list artist-ranking-list">
          <li
            v-for="(item, index) in topTenRanking"
            :key="`new-${item.artist_display || index}`"
            :class="['artist-ranking-list-item', 'ranking-item', { 'artist-ranking-list-item--top': index === 0 }]"
          >
            <div class="artist-ranking-item-main">
              <span class="artist-ranking-list-rank">#{{ item.rank || index + 1 }}</span>
              <div class="artist-ranking-item-copy">
                <strong>{{ item.artist_display || '未知歌手' }}</strong>
                <small>
                  新增专辑 {{ item.new_album_total ?? 0 }} 张
                  <template v-if="item.highlight_tag"> · {{ item.highlight_tag }}</template>
                </small>
              </div>
            </div>
            <span>{{ formatMetric(item) }}</span>
          </li>
        </ol>
      </section>
      <section v-else class="artist-ranking-list-card story-card story-card--focus">
        <strong>榜单暂未生成</strong>
        <p>等年度新增歌手统计补齐后，这里会出现你的年度新增 Top10 榜单。</p>
      </section>
    </div>
  </ReportPageShell>
</template>
```

- [ ] **Step 3: 跑前端单测，确认转绿**

Run:
```bash
npm run test -- src/__tests__/year-report-mobile-app.test.js
```

Workdir: `tools/year-report-app`

Expected:
- PASS
- `L04A / L04B` 断言变绿，其余页面测试不回归。

---

### Task 3: 补纯列表模式的最小样式，避免页面塌陷

**Files:**
- Modify: `tools/year-report-app/src/styles.css`
- Test: `tools/year-report-app/src/pages/L04LibraryArtistRankingPage.vue`
- Test: `tools/year-report-app/src/pages/L04NewArtistRankingPage.vue`

- [ ] **Step 1: 为纯列表模式补首项轻强调样式，并压紧 10 条列表节奏**

```css
.artist-ranking-list-item {
  align-items: flex-start;
  gap: 14px;
}

.artist-ranking-list-item--top {
  padding-top: 2px;
}

.artist-ranking-list-item--top .artist-ranking-list-rank {
  background: var(--page-accent-soft);
  border-color: rgba(122, 132, 166, 0.18);
}

.artist-ranking-list-item--top .artist-ranking-item-copy strong {
  color: var(--page-accent-strong);
}

.artist-ranking-item-copy {
  display: grid;
  gap: 4px;
}

.artist-ranking-item-copy small {
  color: var(--report-text-muted);
  line-height: 1.35;
}
```

- [ ] **Step 2: 再跑前端单测，确认样式调整没有打断结构**

Run:
```bash
npm run test -- src/__tests__/year-report-mobile-app.test.js
```

Workdir: `tools/year-report-app`

Expected:
- PASS
- 结构类名与列表数量保持稳定。

---

### Task 4: 补齐 sample contract，并验证 fallback 数据也是 10 条

**Files:**
- Modify: `tools/year-report-app/public/report-contract.sample.json`

- [ ] **Step 1: 把 `L04A` 样例榜单补到 10 条，保持 `track_total / album_total / top_track_title` 字段完整**

```json
{
  "rank": 6,
  "artist_display": "陈奕迅",
  "track_total": 1,
  "album_total": 1,
  "top_track_title": "K歌之王"
}
```

要求：在现有 1~5 条基础上继续补 `rank: 6 ~ 10`，并保持字段格式一致。

- [ ] **Step 2: 把 `L04B` 样例榜单补到 10 条，保持 `new_track_total / new_album_total / highlight_tag` 字段完整**

```json
{
  "rank": 5,
  "artist_display": "陈奕迅",
  "new_track_total": 1,
  "new_album_total": 1,
  "highlight_tag": "第一次加入曲库"
}
```

要求：在现有 1~4 条基础上继续补 `rank: 5 ~ 10`，并保持字段格式一致。

- [ ] **Step 3: 用脚本校验 sample 榜单长度都为 10**

Run:
```bash
@'
import json
from pathlib import Path
path = Path('tools/year-report-app/public/report-contract.sample.json')
data = json.loads(path.read_text(encoding='utf-8'))
pages = {page['page_id']: page for page in data['pages']}
print('L04A', len(pages['L04A']['payload']['ranking']))
print('L04B', len(pages['L04B']['payload']['ranking']))
'@ | python -
```

Expected:
```text
L04A 10
L04B 10
```

---

### Task 5: 做最终验证并提交

**Files:**
- Modify: `tools/year-report-app/src/__tests__/year-report-mobile-app.test.js`
- Modify: `tools/year-report-app/src/pages/L04LibraryArtistRankingPage.vue`
- Modify: `tools/year-report-app/src/pages/L04NewArtistRankingPage.vue`
- Modify: `tools/year-report-app/src/styles.css`
- Modify: `tools/year-report-app/public/report-contract.sample.json`

- [ ] **Step 1: 跑前端测试全量回归**

Run:
```bash
npm run test -- src/__tests__/year-report-mobile-app.test.js
```

Workdir: `tools/year-report-app`

Expected:
- PASS
- 所有年报移动端页面测试通过。

- [ ] **Step 2: 跑 Vite build，确认 public sample 会正常拷贝到 dist**

Run:
```bash
npm run build
```

Workdir: `tools/year-report-app`

Expected:
- PASS
- 产物正常生成；`dist/report-contract.sample.json` 会随 public 一起更新。

- [ ] **Step 3: 再校验正式 contract 仍为 10 条，确认没有误动真实数据**

Run:
```bash
@'
import json
from pathlib import Path
path = Path('tools/year-report-app/public/report-contract.json')
data = json.loads(path.read_text(encoding='utf-8'))
pages = {page['page_id']: page for page in data['pages']}
print('L04A', len(pages['L04A']['payload']['ranking']))
print('L04B', len(pages['L04B']['payload']['ranking']))
'@ | python -
```

Expected:
```text
L04A 10
L04B 10
```

- [ ] **Step 4: 提交本次实现**

```bash
git add tools/year-report-app/src/__tests__/year-report-mobile-app.test.js tools/year-report-app/src/pages/L04LibraryArtistRankingPage.vue tools/year-report-app/src/pages/L04NewArtistRankingPage.vue tools/year-report-app/src/styles.css tools/year-report-app/public/report-contract.sample.json
git commit -m "feat(year-report-app): render l04 as pure top10 list"
```

---

## 三、自检结果

### Spec coverage

- `L04A / L04B` 改成纯 Top10 列表：Task 1 / Task 2 / Task 3 覆盖；
- sample contract 补齐 10 条：Task 4 覆盖；
- 不改 Python contract：通过“只改 tools/year-report-app”范围约束明确控制；
- 最终验证正式 contract 与 fallback contract：Task 5 覆盖。

### Placeholder scan

- 已避免 `TODO / TBD / similar to Task N`；
- 每一步都附了具体文件、命令和预期输出。

### Type consistency

- 前端继续统一读取 `page.payload.ranking`；
- `L04A` 使用 `track_total / album_total / top_track_title`；
- `L04B` 使用 `new_track_total / new_album_total / highlight_tag`；
- 页面与 sample JSON 字段命名保持一致。
