# 待做需求清单

> 盘点日期：2026-06-02  
> 口径说明：本文件只记录仍需要产品确认或后续开发的事项；历史实现计划里的未勾选步骤不直接视为待办，因为部分计划未在完工后回填勾选状态。

## 当前结论

- 当前没有正式登记的阻塞性待做需求。
- GitHub 当前没有 open issue / open PR。
- 以下条目来自规格文档的“后续演进方向”和源码中的 TODO 注释，默认先作为候选需求或技术债管理，开工前需要重新确认范围、优先级与验收标准。

## 候选产品需求

### 媒体库歌曲详情

来源：`docs/superpowers/specs/2026-05-15-media-library-music-detail-modal-design.md`

- [x] 将媒体库歌曲详情弹窗升级为独立详情页。
  - 处理结果：已新增独立 `MusicDetailPage`，并将「我的列表」中的本地 / 媒体库歌曲详情入口切换为页面展示；旧弹窗源码保留。
- [x] 在搜索页 `LibraryMusicList` 中接入同款详情入口。
  - 处理结果：搜索页文件源 / all 结果列表新增详情入口，本地 / 媒体库歌曲进入应用内详情页，在线音源仍走外链详情。
- [ ] 在详情页中增加缓存状态、来源切换、重新扫描等高级能力。
  - [x] 先展示只读缓存状态。
    - 处理结果：歌曲详情页已展示媒体库歌曲的未缓存、已缓存、缓存已过期、缓存来源、预加载状态、本地缓存路径、文件大小和访问时间等只读信息。
  - [ ] 来源切换。
  - [x] 重新扫描。
    - 处理结果：歌曲详情页已支持提交当前媒体库歌曲所属导入规则的增量重新扫描；页面只提交后台任务，不直接执行远端扫描、删除校验或缓存清理。
  - 目的：把媒体库歌曲排障和修复能力集中到详情场景。
  - 开工前需确认：来源切换等写操作是否允许影响远端或只影响本地索引。
- [ ] 重新梳理菜单中的「分享歌曲」与「复制歌曲名」语义。
  - 目的：减少“分享”和“复制”两个动作在本地 / 在线歌曲上的理解歧义。
  - 开工前需确认：菜单文案、复制格式、分享格式和多语言键。

### 歌手相关歌曲

来源：`docs/superpowers/specs/2026-05-16-media-library-artist-related-songs-design.md`

- [ ] 将当前歌手结果浮层升级为独立歌手页。
  - 目的：承载更多歌手维度信息和操作，而不是只展示当前列表内的临时结果。
  - 开工前需确认：歌手页是否只面向媒体库、是否支持在线源歌曲、是否进入导航栈。
- [ ] 在列表行、副标题或播放页中增加更多歌手入口。
  - 目的：减少只能从详情弹窗进入歌手结果的路径限制。
  - 开工前需确认：点击区域、误触处理、长歌手名展示和多歌手拆分规则。
- [ ] 支持联名歌手拆分与单歌手聚合。
  - 目的：把 `A / B`、`A; B`、`A feat. B` 等协作歌手归到可独立浏览的歌手维度。
  - 开工前需确认：分隔符规则、别名规则、排序规则和兼容旧数据的策略。
- [ ] 扩展为当前媒体库或全部列表范围的歌手歌曲浏览。
  - 目的：突破“只在当前列表内找相关歌曲”的限制。
  - 开工前需确认：默认范围、范围切换 UI、查询性能和空结果提示。
- [ ] 增加歌手维度的排序、播放全部、批量操作。
  - 目的：让歌手结果从只读列表升级为可操作集合。
  - 开工前需确认：排序字段、播放队列插入方式、批量选择与现有多选模式是否复用。

## 待澄清需求

### OneDrive Business 登录

来源：`docs/superpowers/specs/2026-04-06-onedrive-business-auth-design.md`

- [ ] 确认 OneDrive Business 登录的租户范围。
  - 待确认选项：单租户 / 多租户组织账号。
  - 影响范围：Azure 应用注册、MSAL 配置、redirect URI、权限申请和用户文案。
  - 未确认前不建议继续实现 RN 与原生桥接，避免登录架构返工。

## 技术债 / 源码待办清理

源码 TODO 已清理：第一批目标源码不再保留裸 `TODO` / `FIXME` 注释。已经有明确现有行为的项目直接完成；仍需产品语义确认的行为从源码注释收口到下一节统一跟踪。

- [x] 补齐同步来源列表更新时间逻辑。
  - 来源：`src/core/syncSourceList.ts`
  - 处理结果：当前实现已经写入 `setListUpdateTime(...)` 时间戳，本次删除旧版格式化时间字段的过期注释。
- [x] 移除歌词获取链路中的 `any` 类型兜底。
  - 来源：`src/core/music/utils.ts`
  - 处理结果：在线歌词请求与其他 SDK 请求保持一致，直接读取 `getLyric(...).promise`。
- [x] 确认本地歌曲 URL 缓存策略的源码注释归属。
  - 来源：`src/core/music/local.ts`
  - 处理结果：当前仅缓存匹配到的在线候选源 URL，本地歌曲继续优先使用文件路径；后续是否新增本地 URL 持久化见“仍需语义确认”。
- [x] 确认下载封面 URL 持久化策略的源码注释归属。
  - 来源：`src/core/music/download.ts`
  - 处理结果：当前下载项复用 `metadata.musicInfo` 的在线封面缓存；下载列表自身写回策略见“仍需语义确认”。
- [x] 同步列表模块失败状态注释收口。
  - 来源：`src/plugins/sync/client/modules/list/localEvent.ts`
  - 处理结果：当前失败策略是标记模块未就绪并关闭连接；更细状态上报见“仍需语义确认”。
- [x] 同步不喜欢列表模块失败状态注释收口。
  - 来源：`src/plugins/sync/client/modules/dislike/localEvent.ts`
  - 处理结果：当前失败策略与列表模块一致；更细状态上报见“仍需语义确认”。
- [x] 恢复播放信息后的启动自动播放注释收口。
  - 来源：`src/plugins/player/playList.ts`
  - 处理结果：当前恢复播放信息后保持暂停，避免启动阶段自动出声；是否恢复自动播放设置见“仍需语义确认”。
- [x] 默认列表信息持久化注释收口。
  - 来源：`src/types/list.d.ts`
  - 处理结果：类型注释改为说明临时列表只保留最小元信息；默认列表是否扩展持久化见“仍需语义确认”。
- [x] 设置页资源缓存清理的列表缓存注释收口。
  - 来源：`src/screens/Home/Views/Setting/settings/Other/ResourceCache.tsx`
  - 处理结果：当前页面只清理已有明确入口的 App 缓存、播放器缓存、音乐 URL 缓存和权限检查状态；列表缓存清理见“仍需语义确认”。

## 仍需语义确认

这些项目已从源码 TODO 收口到本文档，但不在第一批低风险清理中直接改变行为。

- [ ] 是否需要为本地歌曲额外持久化播放 URL。
  - 需要确认：本地文件路径本身是否已经足够稳定，以及缓存失效条件如何定义。
- [ ] 是否需要把下载封面 URL 写回下载项自身。
  - 需要确认：下载列表是否存在稳定写回入口，以及与 `metadata.musicInfo.meta.picUrl` 的一致性规则。
- [ ] 同步列表 / 不喜欢列表失败时是否需要额外状态上报。
  - 需要确认：状态应展示给 UI、写入日志，还是发给同步对端。
- [ ] 恢复播放信息后是否允许按设置自动播放。
  - 需要确认：启动自动播放设置是否仍保留，以及 Android 音频焦点和后台启动限制。
- [ ] 默认列表信息是否需要进入备份、同步或迁移流程。
  - 需要确认：默认列表元信息的持久化范围，以及与用户列表结构的兼容策略。
- [ ] 设置页资源缓存清理是否要包含列表缓存。
  - 需要确认：列表缓存定义、清理后重建方式和是否需要二次确认。

## 暂不纳入待办的事项

- `docs/superpowers/plans/` 下历史计划中的未勾选步骤暂不视为真实未完成项。
  - 原因：这些计划主要用于实施过程，部分功能已经通过代码、测试、changelog 和 release 落地，但计划文件未同步回填。
- 本地未跟踪的 `docs/superpowers/plans/2026-05-15-media-library-music-detail-modal.md` 暂不视为新需求。
  - 原因：对应媒体库歌曲详情弹窗功能已在当前代码中存在，文件更像历史计划记录或漏提交文档。
